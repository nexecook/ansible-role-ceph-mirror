---

ceph_mirror_cron_disabled: true
ceph_download_region: "global"
ceph_mirror_cron_minute: "{{ 59 | random(seed=inventory_hostname) }}"
ceph_mirror_cron_hour: "{{ 23 | random(seed=inventory_hostname) }}"
